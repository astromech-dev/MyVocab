// The two languages, in one place. Learning a different language later means
// changing these two lines — nothing else in the app hardcodes them.
export const TARGET_LANGUAGE = 'Armenian';   // the language being learned
export const NATIVE_LANGUAGE = 'Russian';    // the language you translate into
